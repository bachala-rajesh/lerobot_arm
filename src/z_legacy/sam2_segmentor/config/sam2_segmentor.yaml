sam2_node:
  ros__parameters:
    # SAM2 model config name (registered inside sam2 Python package)
    model_config: "configs/sam2.1/sam2.1_hiera_l.yaml"

    # Absolute path to the downloaded checkpoint
    checkpoint: "/home/mira/workspaces/lerobot_ws/src/deep_learning_models/models/raw_models/sam2/sam2.1_hiera_large.pt"

    # Camera topic — compressed RGB from OAK-D
    image_topic: "/oak/rgb/image_raw/compressed"

    # "cuda" or "cpu"
    device: "cuda"
